# Copy to .env and fill in. Used by the Vite dev proxy (local) and the
# Vercel serverless function (production) to send the enquiry/quote email.
# In production, set this in the Vercel project's Environment Variables.
BREVO_API_KEY=your-brevo-api-key-here
